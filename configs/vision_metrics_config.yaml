# VisionMetrics Configuration
# Thresholds for alerting when vision metrics fall below acceptable levels

vision_metrics:
  # OCR accuracy thresholds (percentage)
  ocr_accuracy_warning: 0.90    # 90% - Warning level
  ocr_accuracy_critical: 0.80   # 80% - Critical level
  
  # Amount MAE thresholds (in big blinds)
  amount_mae_warning: 1.0       # 1 BB - Warning level  
  amount_mae_critical: 2.0      # 2 BB - Critical level
  
  # Card recognition accuracy thresholds (percentage)
  card_accuracy_warning: 0.95   # 95% - Warning level
  card_accuracy_critical: 0.90  # 90% - Critical level
  
  # Minimum samples before triggering alerts
  # Prevents false alarms from small sample sizes
  min_samples_for_alert: 10

# Vision Timing Profiling Configuration
# Detailed timing logs for performance analysis and optimization
vision_timing:
  # Enable detailed timing profiling (default: false)
  # When enabled, writes detailed timing breakdowns to JSONL log files
  enabled: false
  
  # Directory for timing log files (default: logs/vision_timing)
  log_dir: "logs/vision_timing"
  
  # Custom log filename (optional, default: vision_timing_{timestamp}.jsonl)
  # If not specified, uses auto-generated timestamped filename
  log_filename: null

# Example usage:
# from holdem.vision.vision_metrics import VisionMetrics, VisionMetricsConfig
# from holdem.vision.vision_timing import create_profiler
# import yaml
#
# with open('configs/vision_metrics_config.yaml') as f:
#     config_dict = yaml.safe_load(f)
# 
# # Setup metrics
# config = VisionMetricsConfig(**config_dict['vision_metrics'])
# metrics = VisionMetrics(config=config)
#
# # Setup timing profiler
# timing_config = config_dict.get('vision_timing', {})
# profiler = create_profiler(
#     enabled=timing_config.get('enabled', False),
#     log_dir=timing_config.get('log_dir'),
#     log_filename=timing_config.get('log_filename')
# )
